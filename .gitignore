node_modules
.next
.env
.env.local
.DS_Store
*.log

# TypeScript incremental build cache — regenerated on every build/type-check.
tsconfig.tsbuildinfo

# Local-only legacy archives (source spreadsheet, WhatsApp chat backup,
# etc.) — kept on disk for reference but not appropriate to ship in the repo.
legacy_files/
# Excel auto-saves a lock file alongside any open .xlsx; never commit it.
~$*.xlsx
